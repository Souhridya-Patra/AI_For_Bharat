# Ultra-minimal requirements for hackathon
# Using stable versions with guaranteed pre-built wheels

# Core API - older stable versions
fastapi==0.100.0
uvicorn==0.23.0

# Pydantic v1 (no Rust compilation needed)
pydantic==1.10.13

# AWS SDK
boto3==1.34.34

# Audio
soundfile==0.12.1
librosa==0.10.1

# Utilities
python-multipart==0.0.6
python-dotenv==1.0.0

# Multi-language TTS fallback
gTTS==2.5.0

# XTTS-v2 for ultra-realistic voice cloning
TTS==0.22.0
pydub==0.25.1
