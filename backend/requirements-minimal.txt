# Minimal requirements for quick start
# Using older versions with pre-built wheels to avoid compilation

# Core API (older versions with pre-built wheels)
fastapi==0.104.1
uvicorn==0.24.0
pydantic==2.4.2
pydantic-settings==2.0.3

# AWS SDK (essential)
boto3==1.34.34
botocore==1.34.34

# Audio (pre-built wheels available)
soundfile==0.12.1
librosa==0.10.1

# Authentication (simplified)
python-multipart==0.0.6

# Utilities
python-dotenv==1.0.0

# Multi-language TTS fallback
gTTS==2.5.0

# XTTS-v2 for ultra-realistic voice cloning (optional - large download)
# Uncomment if you want professional-grade synthesis:
# TTS==0.22.0
# pydub==0.25.1
